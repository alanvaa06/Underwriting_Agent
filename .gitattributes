data/chroma/**/*.bin filter=lfs diff=lfs merge=lfs -text
data/chroma/*.sqlite3 filter=lfs diff=lfs merge=lfs -text

# Keep the walkthrough notebook out of GitHub language stats — it is a
# 3 MB doc with embedded outputs, not the app source (FastAPI/LangGraph).
notebooks/** linguist-documentation
*.ipynb linguist-documentation
